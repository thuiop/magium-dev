* current scene = "Ch2-Intro"
+ Counter = 3
	List : Reset
	List : Add line "Her looks"
	bottom of text : Set Counter to 3450
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 24
	Group.Buttons : Destroy
	List : Add line "Deciding which one should make a pass at her"
	List : Add line "Just finishing our discussion from earlier"
	List : Add line "Discussing whether she is a banshee or not"
	Scene text 2 : Display paragraph 3
	Scene text 2 : Set X position to 900
	scene 3 : Display paragraph 3

// * current scene = "Ch2-Intro"
// + First book purchased = 0
// + Counter = 1
	// Ad Requested : Set Counter to 1

* current scene = "Ch2-Intro"
+ Counter = 3
+ checkpoint rich = 0
	Scene text : Set Y position to 40

* current scene = "Ch2-Intro"
+ Counter = 3
+ checkpoint rich = 1
	Successful checks : Set alterable string to "Checkpoint reached: Game saved."

* current scene = "Ch2-Intro"
+ checkpoint rich = 1
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1
	scene 3 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 2" ) - 50
	scene 3 : Set X position to 25

* current scene = "Ch2-Intro"
+ FontSize( "Scene text" ) = 21
+ checkpoint rich = 1
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1
	scene 3 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 2" ) + Text Height( "Calculate Text Rect object 3" ) - 20
	scene 3 : Set X position to 25

* current scene = "Ch2-Intro"
+ FontSize( "Scene text" ) = 18
+ checkpoint rich = 1
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1
	scene 3 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 2" ) + Text Height( "Calculate Text Rect object 3" ) - 20
	scene 3 : Set X position to 25

* current scene = "Ch2-Intro"
+ checkpoint rich = 0
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) - 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1
	scene 3 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 2" ) - 110
	scene 3 : Set X position to 25

* current scene = "Ch2-Intro"
+ FontSize( "Scene text" ) = 21
+ checkpoint rich = 0
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) - 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1
	scene 3 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 2" ) + Text Height( "Calculate Text Rect object 3" ) - 80
	scene 3 : Set X position to 25

* current scene = "Ch2-Intro"
+ FontSize( "Scene text" ) = 18
+ checkpoint rich = 0
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) - 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1
	scene 3 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 2" ) + Text Height( "Calculate Text Rect object 3" ) - 80
	scene 3 : Set X position to 25

// * NOT GPG Connector: Is local player authenticated?
// + enable auto log in = 1
// + Android object : Network is connected?
// + Mouse pointer is over Group.Buttons
// + Counter = 0
// + pressed counter = 2
	// Special : Set enable auto log in to 0
	//  Ini : Set value 0 to item "enable_auto_log_in" in group "achievements"

* NOT GPG Connector: Is local player authenticated?
+ enable auto log in = 1
+ Android object : Network is connected?
+ Mouse pointer is over Group.Buttons
+ cloud achievements = 0
+ Counter = 0
+ pressed counter = 2
	Special : Set enable auto log in to 0
	 Ini : Set value 0 to item "enable_auto_log_in" in group "achievements"

* Always

// * Mouse pointer is over Group.Buttons
// + current scene = "Ch2-Intro"
// + Button ID of Group.Buttons = 1
// + First book purchased = 0
// + Ad timer = 0
// + Counter = 0
// + pressed counter = 2
	// Special : Set Ad timer to 900
	// Ad shown : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Intro"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Looks"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Set CH2 Kate whisper to 1
	Successful checks : Set alterable string to ""
	Scene text : Set Y position to 100
	Special : Set checkpoint rich to 0
	Scene text 2 : Display paragraph 1
	scene 3 : Display paragraph 1

* current scene = "Ch2-Looks"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 850
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 25
	Group.Buttons : Destroy

* current scene = "Ch2-Looks"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Intro"
+ Button ID of Group.Buttons = 2
+ AC CH2 Talker = 0
+ Counter = 0
+ pressed counter = 2
	Special : Set AC CH2 Talker to 1
	achievement timer : Set Counter to 200
	Achievement title : Reappear
	Achievement unlocked : Reappear
	Achievement title : Set alterable string to "Smooth talker"
	Achievement title : Set X position to 327
	Achievement box : Reappear
	Trophy : Reappear

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Intro"
+ Button ID of Group.Buttons = 2
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Pass"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Set CH2 Kate whisper to 2
	Successful checks : Set alterable string to ""
	Scene text : Set Y position to 100
	Special : Set checkpoint rich to 0
	Scene text 2 : Display paragraph 1
	scene 3 : Display paragraph 1

* current scene = "Ch2-Pass"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1100
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 26
	Group.Buttons : Destroy

* current scene = "Ch2-Pass"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

// * Mouse pointer is over Group.Buttons
// + current scene = "Ch2-Intro"
// + Button ID of Group.Buttons = 3
// + First book purchased = 0
// + Ad timer = 0
// + Counter = 0
// + pressed counter = 2
	// Special : Set Ad timer to 900
	// Ad shown : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Intro"
+ Button ID of Group.Buttons = 3
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Earlier"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Set CH2 Kate whisper to 3
	Successful checks : Set alterable string to ""
	Scene text : Set Y position to 100
	Special : Set checkpoint rich to 0
	Scene text 2 : Display paragraph 1
	scene 3 : Display paragraph 1

* current scene = "Ch2-Earlier"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 700
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 27
	Group.Buttons : Destroy

* current scene = "Ch2-Earlier"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

// * Mouse pointer is over Group.Buttons
// + current scene = "Ch2-Intro"
// + Button ID of Group.Buttons = 4
// + First book purchased = 0
// + Ad timer = 0
// + Counter = 0
// + pressed counter = 2
	// Special : Set Ad timer to 900
	// Ad shown : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Intro"
+ Button ID of Group.Buttons = 4
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Banshee"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Set CH2 Kate whisper to 4
	Successful checks : Set alterable string to ""
	Scene text : Set Y position to 100
	Special : Set checkpoint rich to 0
	Scene text 2 : Display paragraph 1
	scene 3 : Display paragraph 1

* current scene = "Ch2-Banshee"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1700
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 28
	Group.Buttons : Destroy

* current scene = "Ch2-Banshee"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Looks"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Escort"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

// * Mouse pointer is over Group.Buttons
// + current scene = "Ch2-Pass"
// + Button ID of Group.Buttons = 1
// + First book purchased = 0
// + Ad timer = 0
// + Counter = 0
// + pressed counter = 2
	// Special : Set Ad timer to 900
	// Ad shown : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Pass"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Escort"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Earlier"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Escort"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Banshee"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Escort"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Escort"
+ Counter = 3
+ CH1 stillwater = 1
	List : Reset
	List : Add line "Ask her what she has against me"
	bottom of text : Set Counter to 1100
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 29
	Group.Buttons : Destroy
	List : Add line "Ask why the two of us should trust her"
	List : Add line "Ask why she's so quick to trust Daren and not me"

* current scene = "Ch2-Escort"
+ Counter = 3
+ CH1 stillwater = 2
	bottom of text : Set Counter to 1550
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 30
	Group.Buttons : Destroy
	List : Reset
	List : Add line "Ask her what she has against me"
	List : Add line "Ask why the two of us should trust her"
	List : Add line "Ask why she's so quick to trust Daren and not me"

* current scene = "Ch2-Escort"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Escort"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Against"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Against"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1900
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 31
	Group.Buttons : Destroy
	Scene text 2 : Display paragraph 4
	Scene text 2 : Set Y position to 900

* current scene = "Ch2-Against"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* current scene = "Ch2-Against"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Against"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Against"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Escort"
+ Button ID of Group.Buttons = 2
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Trust"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Trust"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 2000
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 32
	Group.Buttons : Destroy
	Scene text 2 : Display paragraph 5
	Scene text 2 : Set Y position to 900

* current scene = "Ch2-Trust"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* current scene = "Ch2-Trust"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Trust"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Trust"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Escort"
+ Button ID of Group.Buttons = 3
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Quick"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Quick"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1900
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 33
	Group.Buttons : Destroy
	Scene text 2 : Display paragraph 6
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Quick"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* current scene = "Ch2-Quick"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Quick"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Quick"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Against"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Stats"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Add 3 to Available points
	Special : Add 3 to Available points aux

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Trust"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Stats"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Add 3 to Available points
	Special : Add 3 to Available points aux

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Quick"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Stats"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Add 3 to Available points
	Special : Add 3 to Available points aux

* current scene = "Ch2-Stats"
+ Counter = 3
	List : Reset
	List : Add line "Invest points now"
	bottom of text : Set Counter to 2800
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 34
	Group.Buttons : Destroy
	List : Add line "Invest points later(by pressing the stats button on the top of the screen)"
	Scene text 2 : Display paragraph 7
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Stats"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Stats"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Stats"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Stats"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Stats-spent"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	storyboard controls : Jump to frame "Stats" (57)

* current scene = "Ch2-Stats-spent"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 2800
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 34
	Group.Buttons : Destroy
	Scene text 2 : Display paragraph 7
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Stats-spent"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Stats-spent"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Stats-spent"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Stats"
+ Button ID of Group.Buttons = 2
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Fallen-trees"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Stats-spent"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Fallen-trees"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Fallen-trees"
+ Counter = 3
+ CH1 stillwater = 2
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 3700
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 35
	Group.Buttons : Destroy
	Scene text 2 : Display paragraph 8
	Scene text 2 : Set X position to 900
	scene 3 : Display paragraph 4

* current scene = "Ch2-Fallen-trees"
+ Counter = 3
+ CH1 stillwater = 1
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 3700
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 36
	Group.Buttons : Destroy
	Scene text 2 : Display paragraph 9
	Scene text 2 : Set X position to 900
	scene 3 : Display paragraph 5

* current scene = "Ch2-Fallen-trees"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1
	scene 3 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 2" ) - 50
	scene 3 : Set X position to 25

* current scene = "Ch2-Fallen-trees"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1
	scene 3 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 2" ) + Text Height( "Calculate Text Rect object 3" ) - 20
	scene 3 : Set X position to 25

* current scene = "Ch2-Fallen-trees"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1
	scene 3 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + Text Height( "Calculate Text Rect object 2" ) + Text Height( "Calculate Text Rect object 3" ) - 20
	scene 3 : Set X position to 25

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Fallen-trees"
+ Button ID of Group.Buttons = 1
+ CH1 stillwater = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-stillwater"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Scene text 2 : Display paragraph 1
	scene 3 : Display paragraph 1

* current scene = "Ch2-Battle-stillwater"
+ Counter = 3
+ Premonition > 0
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1800
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 38
	Group.Buttons : Destroy
	Successful checks : Set alterable string to "[ Premonition check successful - level 1 ]"
	Special : Set CH2 toughness happened to 0

* current scene = "Ch2-Battle-stillwater"
+ Counter = 3
+ Premonition = 0
+ Reflexes > 0
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1750
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 39
	Group.Buttons : Destroy
	Successful checks : Set alterable string to NewLine$ + NewLine$ + "[ Reflexes check successful - level 1 ]"
	Failed checks : Set alterable string to "[ Premonition check failed - level 1 ]"
	Special : Set CH2 toughness happened to 0

* current scene = "Ch2-Battle-stillwater"
+ Counter = 3
+ Premonition = 0
+ Reflexes = 0
+ AC CH2 Nails = 0
+ Toughness > 2
	Special : Set AC CH2 Nails to 1
	Achievement box : Reappear
	Trophy : Reappear
	Achievement unlocked : Reappear
	Achievement title : Reappear
	Achievement title : Set alterable string to "Tough as nails"
	Achievement title : Set X position to 325
	achievement timer : Set Counter to 200

* current scene = "Ch2-Battle-stillwater"
+ Counter = 3
+ Premonition = 0
+ Reflexes = 0
+ Toughness > 2
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1800
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 40
	Group.Buttons : Destroy
	Successful checks : Set alterable string to NewLine$ + NewLine$ + NewLine$ + NewLine$ + "[ Toughness check successful - level 3 ]"
	Failed checks : Set alterable string to "[ Premonition check failed - level 1 ]" + NewLine$ + NewLine$ + "[ Reflexes check failed - level 1 ]"
	Special : Set CH2 toughness happened to 1

* current scene = "Ch2-Battle-stillwater"
+ Counter = 3
+ Premonition = 0
+ Reflexes = 0
+ Toughness < 3
	List : Reset
	List : Add line "Restart game"
	bottom of text : Set Counter to 1000
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 41
	Group.Buttons : Destroy
	Failed checks : Set alterable string to "[ Premonition check failed - level 1 ]" + NewLine$ + NewLine$ + "[ Reflexes check failed - level 1 ]" + NewLine$ + NewLine$ + "[ Toughness check failed - level 3 ]"
	List : Add line "Load from last checkpoint"
	Special : Set is dead to 1
	List : Add line "Load game"

* current scene = "Ch2-Battle-stillwater"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-stillwater"
+ Button ID of Group.Buttons = 1
+ is dead = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch1-Intro1"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Start loop "reset variables" 1 times
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""
	storyboard controls : Jump to frame "Chapter 1" (1)

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-stillwater"
+ Button ID of Group.Buttons = 2
+ is dead = 1
+ Counter = 0
+ pressed counter = 2
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""
	Special : Set chapter load to 1
	Special : Set chapter save counter to 5
	storyboard controls : Jump to frame "Save load game" (55)

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-stillwater"
+ Button ID of Group.Buttons = 3
+ is dead = 1
+ Counter = 0
+ pressed counter = 2
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""
	storyboard controls : Jump to frame "Save load game" (55)

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-stillwater"
+ Button ID of Group.Buttons = 1
+ is dead = 0
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-begins"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Fallen-trees"
+ Button ID of Group.Buttons = 1
+ CH1 stillwater = 2
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-human"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Scene text 2 : Display paragraph 1
	scene 3 : Display paragraph 1
	Special : Set CH2 toughness happened to 0

* current scene = "Ch2-Battle-human"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1300
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 37
	Group.Buttons : Destroy

* current scene = "Ch2-Battle-human"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-human"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-begins"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Battle-begins"
+ Counter = 3
	List : Reset
	List : Add line "Hide behind Daren and let him handle them both"
	bottom of text : Set Counter to 1500
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 42
	Group.Buttons : Destroy
	List : Add line "Draw the younger mage away from Daren and focus on avoiding his attacks"
	List : Add line "Fight the younger mage myself"

* current scene = "Ch2-Battle-begins"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-begins"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Hide"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Hide"
+ Counter = 3
	List : Reset
	List : Add line "Ask Kate for help"
	bottom of text : Set Counter to 2500
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 43
	Group.Buttons : Destroy
	List : Add line "Draw the younger mage away from Daren and focus on avoiding his attacks"
	List : Add line "Fight the younger mage myself"
	Scene text 2 : Display paragraph 10
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Hide"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Hide"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Hide"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-begins"
+ Button ID of Group.Buttons = 2
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Avoid"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Hide"
+ Button ID of Group.Buttons = 2
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Avoid"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Avoid"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 2100
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 45
	Group.Buttons : Destroy
	Scene text 2 : Display paragraph 11
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Avoid"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Avoid"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Avoid"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Avoid"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Avoid2"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Avoid2"
+ Counter = 3
+ Agility > 0
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 2350
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 46
	Group.Buttons : Destroy
	Successful checks : Set alterable string to "[ Speed check successful - level 1 ]"
	Scene text 2 : Display paragraph 12
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Avoid2"
+ Counter = 3
+ Agility = 0
+ Reflexes > 0
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 2550
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 47
	Group.Buttons : Destroy
	Successful checks : Set alterable string to NewLine$ + NewLine$ + "[ Reflexes check successful - level 1 ]"
	Failed checks : Set alterable string to "[ Speed check failed - level 1 ]"
	Scene text 2 : Display paragraph 13
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Avoid2"
+ Counter = 3
+ Agility = 0
+ Reflexes = 0
+ AC CH2 Nails = 0
+ Toughness > 2
+ CH2 toughness happened = 0
	Special : Set AC CH2 Nails to 1
	Achievement box : Reappear
	Trophy : Reappear
	Achievement unlocked : Reappear
	Achievement title : Reappear
	Achievement title : Set alterable string to "Tough as nails"
	Achievement title : Set X position to 325
	achievement timer : Set Counter to 200

* current scene = "Ch2-Avoid2"
+ Counter = 3
+ Agility = 0
+ Reflexes = 0
+ Toughness > 2
+ CH2 toughness happened = 0
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 3050
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 48
	Group.Buttons : Destroy
	Successful checks : Set alterable string to NewLine$ + NewLine$ + NewLine$ + NewLine$ + "[ Toughness check successful - level 3 ]"
	Failed checks : Set alterable string to "[ Speed check failed - level 1 ]" + NewLine$ + NewLine$ + "[ Reflexes check failed - level 1 ]"
	Scene text 2 : Display paragraph 14
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Avoid2"
+ Counter = 3
+ Agility = 0
+ Reflexes = 0
+ Toughness > 2
+ CH2 toughness happened = 1
	bottom of text : Set Counter to 1450
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 49
	Group.Buttons : Destroy
	Failed checks : Set alterable string to "[ Speed check failed - level 1 ]" + NewLine$ + NewLine$ + "[ Reflexes check failed - level 1 ]"
	List : Reset
	List : Add line "Restart game"
	List : Add line "Load from last checkpoint"
	Special : Set is dead to 1
	List : Add line "Load game"

* current scene = "Ch2-Avoid2"
+ Counter = 3
+ Agility = 0
+ Reflexes = 0
+ Toughness < 3
	bottom of text : Set Counter to 1450
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 50
	Group.Buttons : Destroy
	Failed checks : Set alterable string to "[ Speed check failed - level 1 ]" + NewLine$ + NewLine$ + "[ Reflexes check failed - level 1 ]" + NewLine$ + NewLine$ + "[ Toughness check failed - level 3 ]"
	Special : Set is dead to 1
	List : Reset
	List : Add line "Restart game"
	List : Add line "Load from last checkpoint"
	List : Add line "Load game"

* current scene = "Ch2-Avoid2"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Avoid2"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Avoid2"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Avoid2"
+ Button ID of Group.Buttons = 1
+ is dead = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch1-Intro1"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Start loop "reset variables" 1 times
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""
	storyboard controls : Jump to frame "Chapter 1" (1)

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Avoid2"
+ Button ID of Group.Buttons = 2
+ is dead = 1
+ Counter = 0
+ pressed counter = 2
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""
	Special : Set chapter load to 1
	Special : Set chapter save counter to 5
	storyboard controls : Jump to frame "Save load game" (55)

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Avoid2"
+ Button ID of Group.Buttons = 3
+ is dead = 1
+ Counter = 0
+ pressed counter = 2
	storyboard controls : Jump to frame "Save load game" (55)

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-begins"
+ Button ID of Group.Buttons = 3
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Fight"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Hide"
+ Button ID of Group.Buttons = 3
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Fight"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Fight"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 2100
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 51
	Group.Buttons : Destroy
	Scene text 2 : Display paragraph 15
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Fight"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* current scene = "Ch2-Fight"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Fight"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Fight"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Fight"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Fight2"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Fight2"
+ Counter = 3
+ Combat technique > 0
+ AC CH2 Master = 0
	Special : Set AC CH2 Master to 1
	Achievement box : Reappear
	Trophy : Reappear
	Achievement unlocked : Reappear
	Achievement title : Reappear
	Achievement title : Set alterable string to "Martial arts master"
	Achievement title : Set X position to 300
	achievement timer : Set Counter to 200

* current scene = "Ch2-Fight2"
+ Counter = 3
+ Combat technique > 0
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 2550
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 52
	Group.Buttons : Destroy
	Successful checks : Set alterable string to "[ Combat technique check successful - level 1 ]"
	Scene text 2 : Display paragraph 16
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Fight2"
+ Counter = 3
+ Combat technique = 0
+ Toughness > 2
+ AC CH2 Nails = 0
+ CH2 toughness happened = 0
	Special : Set AC CH2 Nails to 1
	Achievement box : Reappear
	Trophy : Reappear
	Achievement unlocked : Reappear
	Achievement title : Reappear
	Achievement title : Set alterable string to "Tough as nails"
	Achievement title : Set X position to 325
	achievement timer : Set Counter to 200

* current scene = "Ch2-Fight2"
+ Counter = 3
+ Combat technique = 0
+ Toughness > 2
+ CH2 toughness happened = 0
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 2750
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 53
	Group.Buttons : Destroy
	Successful checks : Set alterable string to NewLine$ + NewLine$ + "[ Toughness check successful - level 3 ]"
	Failed checks : Set alterable string to "[ Combat technique check failed - level 1 ]"
	Scene text 2 : Display paragraph 17
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Fight2"
+ Counter = 3
+ Combat technique = 0
+ Toughness > 2
+ CH2 toughness happened = 1
	bottom of text : Set Counter to 1100
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 54
	Group.Buttons : Destroy
	Failed checks : Set alterable string to "[ Combat technique check failed - level 1 ]"
	List : Reset
	List : Add line "Restart game"
	List : Add line "Load from last checkpoint"
	Special : Set is dead to 1
	List : Add line "Load game"

* current scene = "Ch2-Fight2"
+ Counter = 3
+ Combat technique = 0
+ Toughness < 3
	bottom of text : Set Counter to 1100
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 55
	Group.Buttons : Destroy
	Failed checks : Set alterable string to "[ Combat technique check failed - level 1 ]" + NewLine$ + NewLine$ + "[ Toughness check failed - level 3 ]"
	Special : Set is dead to 1
	List : Reset
	List : Add line "Restart game"
	List : Add line "Load from last checkpoint"
	List : Add line "Load game"

* current scene = "Ch2-Fight2"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Fight2"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Fight2"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Fight2"
+ Button ID of Group.Buttons = 1
+ is dead = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch1-Intro1"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Start loop "reset variables" 1 times
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""
	storyboard controls : Jump to frame "Chapter 1" (1)

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Fight2"
+ Button ID of Group.Buttons = 2
+ is dead = 1
+ Counter = 0
+ pressed counter = 2
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""
	Special : Set chapter load to 1
	Special : Set chapter save counter to 5
	storyboard controls : Jump to frame "Save load game" (55)

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Fight2"
+ Button ID of Group.Buttons = 3
+ is dead = 1
+ Counter = 0
+ pressed counter = 2
	storyboard controls : Jump to frame "Save load game" (55)

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Hide"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Ask-Kate"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Set CH2 Kate debt to 1
	Scene text 2 : Display paragraph 1
	scene 3 : Display paragraph 1

* current scene = "Ch2-Ask-Kate"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1500
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 44
	Group.Buttons : Destroy

* current scene = "Ch2-Ask-Kate"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Ask-Kate"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-end"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Avoid2"
+ Button ID of Group.Buttons = 1
+ is dead = 0
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-end"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Failed checks : Set alterable string to ""
	Successful checks : Set alterable string to ""
	Scene text 2 : Display paragraph 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Fight2"
+ Button ID of Group.Buttons = 1
+ is dead = 0
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-end"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Failed checks : Set alterable string to ""
	Successful checks : Set alterable string to ""
	Scene text 2 : Display paragraph 1

* current scene = "Ch2-Battle-end"
+ Counter = 3
+ CH1 stillwater = 1
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1700
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 56
	Group.Buttons : Destroy

* current scene = "Ch2-Battle-end"
+ Counter = 3
+ CH1 stillwater = 2
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1400
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 57
	Group.Buttons : Destroy

* current scene = "Ch2-Battle-end"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-end"
+ Button ID of Group.Buttons = 1
+ CH1 stillwater = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Confession"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Confession"
+ Counter = 3
	List : Reset
	List : Add line "Tell him the truth"
	bottom of text : Set Counter to 2200
	List : Add line "Tell him I was forced into it"
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 58
	Group.Buttons : Destroy
	List : Add line "Tell him I'm looking for a cure for my sick sister"
	Scene text 2 : Display paragraph 18
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Confession"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* current scene = "Ch2-Confession"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Confession"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Confession"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Confession"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Truth2"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Set CH2 human reason to 1
	Scene text 2 : Display paragraph 1

* current scene = "Ch2-Truth2"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1750
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 59
	Group.Buttons : Destroy

* current scene = "Ch2-Truth2"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Confession"
+ Button ID of Group.Buttons = 2
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Forced2"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Set CH2 human reason to 2

* current scene = "Ch2-Forced2"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 2200
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 60
	Group.Buttons : Destroy
	Scene text 2 : Display paragraph 19
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Forced2"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Forced2"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Forced2"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Confession"
+ Button ID of Group.Buttons = 3
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Sister2"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Set CH2 human reason to 3
	Scene text 2 : Display paragraph 1

* current scene = "Ch2-Sister2"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 1700
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 61
	Group.Buttons : Destroy

* current scene = "Ch2-Sister2"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-end"
+ Button ID of Group.Buttons = 1
+ CH1 stillwater = 2
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-end2"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Truth2"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-end2"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Forced2"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-end2"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Scene text 2 : Display paragraph 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Sister2"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Battle-end2"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Battle-end2"
+ Counter = 3
+ CH2 Kate debt = 0
	List : Reset
	List : Add line """Crystal."""
	bottom of text : Set Counter to 1200
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 62
	Group.Buttons : Destroy
	List : Add line """Not really, could you elaborate some more?"""

* current scene = "Ch2-Battle-end2"
+ Counter = 3
+ CH2 Kate debt = 1
	bottom of text : Set Counter to 1200
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 63
	Group.Buttons : Destroy
	List : Reset
	List : Add line """Don't worry, I always pay my debts."""
	List : Add line """Yes, we're clear."""
	List : Add line """I don't owe you anything."""

* current scene = "Ch2-Battle-end2"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-end2"
+ Button ID of Group.Buttons = 1
+ CH2 Kate debt = 0
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Crystal"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Crystal"
+ Counter = 3
	List : Reset
	List : Add line "Continue"
	bottom of text : Set Counter to 400
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 64
	Group.Buttons : Destroy

* current scene = "Ch2-Crystal"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-end2"
+ Button ID of Group.Buttons = 2
+ CH2 Kate debt = 0
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Elaborate"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Elaborate"
+ Counter = 3
	bottom of text : Set Counter to 600
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 65
	Group.Buttons : Destroy
	List : Reset
	List : Add line "Continue"

* current scene = "Ch2-Elaborate"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-end2"
+ Button ID of Group.Buttons = 1
+ CH2 Kate debt = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Always"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Always"
+ Counter = 3
	bottom of text : Set Counter to 600
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 66
	Group.Buttons : Destroy
	List : Reset
	List : Add line "Continue"

* current scene = "Ch2-Always"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-end2"
+ Button ID of Group.Buttons = 2
+ CH2 Kate debt = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Clear"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Clear"
+ Counter = 3
	bottom of text : Set Counter to 400
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 67
	Group.Buttons : Destroy
	List : Reset
	List : Add line "Continue"

* current scene = "Ch2-Clear"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Battle-end2"
+ Button ID of Group.Buttons = 3
+ CH2 Kate debt = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-No-debt"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Special : Set CH2 not owe debt to 1

* current scene = "Ch2-No-debt"
+ Counter = 3
	bottom of text : Set Counter to 700
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 68
	Group.Buttons : Destroy
	List : Reset
	List : Add line "Continue"

* current scene = "Ch2-No-debt"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Crystal"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Deer"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Elaborate"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Deer"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Always"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Deer"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Clear"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Deer"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-No-debt"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Deer"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0

* current scene = "Ch2-Deer"
+ Counter = 3
+ Ancient languages < 2
	bottom of text : Set Counter to 700
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 69
	Group.Buttons : Destroy
	Failed checks : Set alterable string to "[ Ancient languages check failed - level 2 ]"
	List : Reset
	List : Add line "Next chapter"
	Special : Set CH2 understood deer to 0
	Special : Set CH2 deer interaction to 0

* current scene = "Ch2-Deer"
+ Counter = 3
+ Ancient languages >= 2
	bottom of text : Set Counter to 1100
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 70
	Group.Buttons : Destroy
	List : Reset
	List : Add line "Offer to provide protection in exchange for leading us to their cave"
	List : Add line "Wish them safe travels"
	List : Add line "Tell them they look delicious"
	Special : Set CH2 understood deer to 1
	Successful checks : Set alterable string to "[ Ancient languages check successful - level 2 ]"

* current scene = "Ch2-Deer"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Deer"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ CH2 understood deer = 0
+ pressed counter = 2
	 Ini : Set value 1 to item "next_chapter_crash" in group "achievements"
	Special : Set next chapter crash to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Protection"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set next chapter crash to 1
	 Ini : Set value 1 to item "next_chapter_crash" in group "achievements"

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Travels"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set next chapter crash to 1
	 Ini : Set value 1 to item "next_chapter_crash" in group "achievements"

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Delicious"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set next chapter crash to 1
	 Ini : Set value 1 to item "next_chapter_crash" in group "achievements"

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Deer"
+ Button ID of Group.Buttons = 1
+ CH2 understood deer = 0
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch3-Intro"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Failed checks : Set alterable string to ""
	Special : Set chapter save to 1
	Special : Set chapter save counter to 5
	storyboard controls : Jump to frame "Save load game" (55)
	Special : Set checkpoint rich to 1
	Successful checks : Set alterable string to ""

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Deer"
+ Button ID of Group.Buttons = 1
+ CH2 understood deer = 1
+ AC CH2 Unlikely = 0
+ Counter = 0
+ pressed counter = 2
	Special : Set AC CH2 Unlikely to 1
	Achievement box : Reappear
	Trophy : Reappear
	Achievement unlocked : Reappear
	Achievement title : Reappear
	Achievement title : Set alterable string to "An unlikely ally"
	Achievement title : Set X position to 320
	achievement timer : Set Counter to 200

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Deer"
+ Button ID of Group.Buttons = 1
+ CH2 understood deer = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Protection"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""
	Special : Set CH2 deer interaction to 1

* current scene = "Ch2-Protection"
+ Counter = 3
	bottom of text : Set Counter to 2300
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 71
	Group.Buttons : Destroy
	List : Reset
	List : Add line "Next chapter"
	Scene text 2 : Display paragraph 20
	Scene text 2 : Set X position to 900

* current scene = "Ch2-Protection"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 20
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Protection"
+ FontSize( "Scene text" ) = 21
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* current scene = "Ch2-Protection"
+ FontSize( "Scene text" ) = 18
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times
	Scene text 2 : Set Y position to Text Height( "Calculate Text Rect object" ) + Text Height( "Calculate Text Rect object 3" ) + 40
	Scene text 2 : Set X position to 25
	double scene : Set Counter to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Deer"
+ Button ID of Group.Buttons = 2
+ CH2 understood deer = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Travels"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""
	Special : Set CH2 deer interaction to 2

* current scene = "Ch2-Travels"
+ Counter = 3
	bottom of text : Set Counter to 1200
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 72
	Group.Buttons : Destroy
	List : Reset
	List : Add line "Next chapter"

* current scene = "Ch2-Travels"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Deer"
+ Button ID of Group.Buttons = 3
+ CH2 understood deer = 1
+ Counter = 0
+ pressed counter = 2
	Special : Set current scene to "Ch2-Delicious"
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Successful checks : Set alterable string to ""
	Failed checks : Set alterable string to ""
	Special : Set CH2 deer interaction to 3

* current scene = "Ch2-Delicious"
+ Counter = 3
	bottom of text : Set Counter to 1200
	storyboard controls : Center display at Y=Y Top( "Scene text" )
	Button Name : Destroy
	Scene text : Display paragraph 73
	Group.Buttons : Destroy
	List : Reset
	List : Add line "Next chapter"

* current scene = "Ch2-Delicious"
+ Counter = 1
	Special : Start loop "buttons" List Nb Lines( "List" ) times

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Protection"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Failed checks : Set alterable string to ""
	Special : Set current scene to "Ch3-Intro"
	Special : Set chapter save to 1
	Special : Set chapter save counter to 5
	storyboard controls : Jump to frame "Save load game" (55)
	Successful checks : Set alterable string to ""
	Special : Set checkpoint rich to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Travels"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Failed checks : Set alterable string to ""
	Special : Set current scene to "Ch3-Intro"
	Special : Set chapter save to 1
	Special : Set chapter save counter to 5
	storyboard controls : Jump to frame "Save load game" (55)
	Successful checks : Set alterable string to ""
	Special : Set checkpoint rich to 1

* Mouse pointer is over Group.Buttons
+ current scene = "Ch2-Delicious"
+ Button ID of Group.Buttons = 1
+ Counter = 0
+ pressed counter = 2
	Counter : Set Counter to 5
	Multiple Touch : Set ScrollSpeed to 0
	Failed checks : Set alterable string to ""
	Special : Set current scene to "Ch3-Intro"
	Special : Set chapter save to 1
	Special : Set chapter save counter to 5
	storyboard controls : Jump to frame "Save load game" (55)
	Successful checks : Set alterable string to ""
	Special : Set checkpoint rich to 1

* Mouse pointer is over Group.Buttons
+ pressed counter = 2
	pressed counter : Set Counter to 0

* Counter = 3
	Scene text 2 : Set X position to 900
	scene 3 : Set X position to 900

